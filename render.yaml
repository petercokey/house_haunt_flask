services:
  - type: web
    name: house-haunt-flask
    env: python

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

    buildCommand: |
      pip install -r requirements.txt
      flask db upgrade

    startCommand: gunicorn run:app


